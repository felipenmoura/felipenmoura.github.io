{
  "name": "felipenmoura",
  "description": "Felipe N. Moura's home page",
  "dependencies": {
    "cpy": "^6.0.0",
    "fs-extra": "^4.0.2",
    "grunt": "*",
    "grunt-contrib-sass": ">=0.4.0",
    "grunt-contrib-watch": "*"
  },
  "scripts": {
    "start": "http-server -p 8080 ./public",
    "build": "grunt build && dsw && mv dsw.js public/dsw.js",
    "publish": "git push gh `git subtree split --prefix public src`:master --force",
    "watch": "grunt watch"
  },
  "engine": "node 0.4.1",
  "devDependencies": {
    "dsw": "^1.12.2",
    "grunt-contrib-cssmin": "^0.12.2",
    "grunt-contrib-uglify": "^0.9.1",
    "grunt-jslint": "*",
    "grunt-nunjucks": "^0.1.4",
    "grunt-scss-lint": "^0.3.4",
    "nunjucks": "^1.2.0"
  }
}
